import pyrtl
from ltspice_pyrtl_lib import half_adder

pyrtl.reset_working_block()

A = pyrtl.Input(1, "A")
B = pyrtl.Input(1, "B")

SUM = pyrtl.Output(1, "SUM")
CARRY = pyrtl.Output(1, "CARRY")

s, c = half_adder(A, B, name="ha0")

SUM <<= s
CARRY <<= c

print(pyrtl.working_block())
